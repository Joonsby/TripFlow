# TripFlow 작업 및 커밋 규칙

## 커밋 메시지

- 커밋 메시지는 `타입 : 한글 설명` 형식으로 작성한다.
- 타입은 `feat`, `fix`, `refactor`, `docs`, `test`, `chore` 등 Conventional Commits의 의미에 맞게 선택한다.
- 설명은 해당 커밋이 완성하는 기능이나 변경을 간결한 한글로 적는다.

## 커밋 단위

- 프론트엔드(`frontend/`)와 백엔드(`backend/`)의 변경은 한 커밋에 섞지 않는다.
- 각 커밋은 해당 영역만 체크아웃해도 빌드되고 실행 가능한 상태여야 한다.
- 공통 문서나 저장소 설정은 프론트엔드·백엔드 기능 커밋과 분리한다.
- 하나의 기능이 양쪽을 모두 변경하더라도 백엔드 API를 먼저 독립 커밋하고, 그 다음에 해당 API를 사용하는 프론트엔드를 별도 커밋한다.
- 관계없는 기능, 버그 수정, 설정 변경은 각각 되돌릴 수 있도록 별도 커밋한다.

## 커밋 전 검증

- 프론트엔드 커밋 전에 `frontend/`에서 `npm run lint`와 `npm run build`를 실행한다.
- 백엔드 커밋 전에 `backend/`에서 `./gradlew test`를 실행한다.
- 일부 파일만 스테이징한 뒤 `git diff --cached --check`와 `git diff --cached --name-only`로 커밋 범위와 공백 오류를 확인한다.
- 환경 제약으로 검증하지 못한 경우에는 그 사유를 커밋 결과와 함께 명시한다.

## 프론트엔드 페이지 레이아웃

- 새로 만드는 모든 페이지의 콘텐츠 영역은 헤더의 `.header-nav`와 같은 수평 규격을 사용한다.
- 헤더 바로 아래 본문 상단 여백은 데스크톱·태블릿에서 32px, 820px 이하 모바일에서 24px를 기본값으로 사용한다.
- 별도의 시각적 이유가 없는 한 페이지별로 헤더와 본문 사이에 기본값보다 큰 상단 여백을 추가하지 않는다.
- 데스크톱과 태블릿에서는 콘텐츠를 가운데 정렬하고 `width: min(1700px, calc(100% - 56px))`를 기준으로 좌우 여백을 각각 28px 확보한다.
- 화면 너비가 820px 이하일 때는 `width: calc(100% - 28px)`를 기준으로 좌우 여백을 각각 14px 확보한다.
- 페이지별로 임의의 `max-width`나 `100vw` 기반 폭을 추가해 헤더와 좌우 경계가 달라지게 하지 않는다.
- 기존 공통 페이지 컨테이너가 있으면 이를 재사용하고, 필요한 경우 헤더 규격을 따르는 공통 레이아웃 클래스로 확장한다.
- 카드, 폼, 지도 등의 내부 콘텐츠는 `width: 100%`, `max-width: 100%`, `min-width: 0`, `box-sizing: border-box`를 적용해 컨테이너 밖으로 넘치거나 가로 스크롤을 만들지 않도록 한다.

## 공통 컴포넌트 재사용

- 두 화면 이상에서 동일하거나 유사한 구조·동작·스타일을 사용하는 UI는 화면별로 중복 구현하지 않고 공통 컴포넌트로 분리한다.
- 새 기능을 구현하기 전에 기존 공통 컴포넌트와 스타일을 먼저 확인하고, 요구사항을 충족할 수 있으면 반드시 재사용한다.
- 기존 공통 컴포넌트로 대부분 표현할 수 있는 경우 화면별 복제 대신 props, children, 슬롯, 확장 className으로 확장한다.
- 반복되는 UI를 새롭게 공통화했다고 판단한 경우 해당 컴포넌트의 이름, 책임, 사용 기준을 이 규칙에 추가하고 이후 동일 유형 작업에서 우선 활용한다.
- 계정 이름·이메일·아바타를 표시하는 프로필 정보 카드는 공통 `AccountProfileCard`를 사용하며 여행자·호스트 마이페이지에서 함께 재사용한다.
- 모바일 서비스형 카드는 공통 `MobileServiceCard`를 사용하며 상단 정보·본문·하단 요약 슬롯에 화면별 콘텐츠를 전달한다.
- 비밀번호와 비밀번호 확인 입력은 공통 `PasswordFields`를 사용하며 회원가입·비밀번호 초기화·마이페이지 비밀번호 변경에서 함께 재사용한다.
- 호스트 모바일 주 메뉴는 공통 `HostHeader`의 하단 내비게이션을 사용하고 계정 관련 보조 동작은 `더보기` 바텀시트에 배치한다.
- 사이드바에서 열림·닫힘 상태를 가지는 액션은 공통 `SidebarActionButton`을 사용하고 `isActive`와 `activeBorder`로 활성 테두리 여부를 제어한다.
- 드롭다운형 사이드바 버튼은 다시 누르면 닫히면서 활성 테두리가 기본값으로 복귀해야 하며, 테마 전환처럼 즉시 실행되는 버튼은 활성 테두리를 사용하지 않는다.
- 메뉴, 버튼, 상태 표시 등에 시각 아이콘이 필요하면 CSS 도형이나 글꼴 문자를 새로 만들지 않고 이미지 생성 도구로 일관된 래스터 이미지 에셋을 제작해 사용한다.
- 같은 화면이나 기능군의 이미지 아이콘은 동일한 프롬프트 규격으로 제작하되, 스프라이트로 합치지 않고 아이콘마다 독립된 투명 이미지 파일로 생성해 프로젝트 `assets`에 저장한다.
- 생성된 개별 아이콘은 CSS 배경 위치로 자르지 않고 `<img>`와 `object-fit: contain`을 사용해 표시한다.
- 생성 이미지 아이콘은 충분한 내부 여백과 투명 배경을 포함해야 하며 작은 화면에서 잘림·넘침이 없는지 컨테이너 크기와 배경 위치를 함께 검증한다.

## 호스트 숙소 관리 정책

- 데이터 관계는 `호스트 계정 1 : N 숙소`, `숙소 1 : N 객실`, `객실 1 : N 예약`으로 설계한다.
- 초기 서비스 화면에서는 호스트 계정당 숙소를 최대 1개만 등록할 수 있게 한다.
- 숙소 등록 제한은 화면에서만 숨기지 않고, 백엔드 구현 시에도 동일하게 검증한다.
- 숙소 조회 API는 향후 다중 숙소로 확장할 수 있도록 단일 객체가 아닌 목록 형태를 기준으로 설계한다.
- 사업자 소재지와 실제 숙소 주소는 다를 수 있으므로 서로 다른 정보로 저장한다.
- 등록된 숙소가 없으면 대시보드와 숙소 관리 화면에 숙소 등록 빈 상태를 표시한다.
- 등록된 숙소가 없으면 객실 등록을 허용하지 않고 숙소 등록 화면으로 안내한다.
- 숙소 등록은 `기본 정보 → 위치 → 시설 및 이용 정보 → 사진 → 확인`의 단계형 화면을 기본으로 한다.
- 숙소 상태는 초기 범위에서 `DRAFT`, `ACTIVE`, `INACTIVE`만 사용한다.
- 결제, 정산, 관리자 검수, 성수기 요금 및 실시간 재고 연동은 초기 숙소 관리 화면 범위에서 제외한다.
- 숙소가 등록된 호스트 대시보드는 오늘 체크인·체크아웃·예약 객실·월 매출 요약, 오늘 일정, 숙소 상태, 최근 예약 순서로 구성한다.
- 숙소 관리 화면은 등록 한도, 운영 상태, 대표 이미지, 객실·예약 요약, 게스트 미리보기와 정보 수정 동작을 제공한다.
- 객실 관리 화면은 전체·판매·중지·예약 가능 수치와 객실별 이미지, 인원, 가격, 재고, 판매 상태를 제공한다.
- 예약 관리 화면은 상태별 요약과 필터, 검색, 예약번호·게스트·객실·일정·인원·금액·상태가 포함된 목록을 제공한다.
- 운영 데이터 표는 모바일에서 가로 스크롤에만 의존하지 않고 각 열 이름이 표시되는 카드형 목록으로 전환한다.

## 반응형 및 모바일 내비게이션

- 새 화면은 데스크톱뿐 아니라 820px 이하 모바일 레이아웃을 함께 구현한다.
- 모바일 카드의 여백, 테두리, 모서리, 그림자를 화면별로 중복 구현하지 않으며 공통 컴포넌트로 표현할 수 없는 요구사항이 있을 때만 확장 클래스나 컴포넌트 변경을 사용한다.
- 모바일에서 그리드와 가로형 컴포넌트는 세로 흐름으로 재배치하고 가로 스크롤이 생기지 않게 한다.
- 모바일 헤더 메뉴는 오른쪽 슬라이드 사이드바로 표시하며 `헤더 → 주요 메뉴 → 모드 전환 → 사용자 프로필` 순서로 세로 배치한다.
- 사이드바에는 배경 오버레이와 닫기 버튼을 제공하고, 오버레이 클릭·메뉴 선택·Escape 입력으로 닫을 수 있게 한다.
- 사이드바가 열린 동안 본문 스크롤을 잠그고 `prefers-reduced-motion` 환경에서는 전환 애니메이션을 제거한다.
- 사이드바의 모드 전환 버튼과 프로필 버튼은 동일한 전체 너비·높이·테두리 규격을 사용한다.
- 사이드바 하단의 프로필 드롭다운은 화면 아래로 넘치지 않도록 프로필 버튼 위쪽으로 펼친다.
- 호스트 사용자가 여행자 화면을 이용할 때 프로필 메뉴에 `호스트 모드로 전환`을 표시하고 호스트 대시보드로 이동시킨다.
- 820px 이하 호스트 화면은 `대시보드 · 숙소 · 객실 · 예약 · 더보기` 순서의 고정 하단 내비게이션을 사용하고 상단 햄버거 사이드바는 표시하지 않는다.
- `더보기`는 새 페이지 대신 바텀시트로 표시하고 호스트 마이페이지, 여행자 모드 전환, 로그아웃을 제공한다.
- 호스트 마이페이지는 `/host/profile`에서 호스트 레이아웃과 모드를 유지하며, 여행자 모드 전환을 명시적으로 선택했을 때만 여행자 화면으로 이동한다.

## 단계형 폼 및 입력 검증

- 단계 번호와 단계명은 진행 상태 표시이며 클릭으로 단계를 이동시키지 않는다.
- 단계 이동은 하단의 `이전`과 `다음` 버튼으로만 처리하고 첫 단계의 `이전` 버튼은 비활성화한다.
- 마지막 확인 단계의 `수정`으로 특정 단계에 진입한 경우 수정 모드를 별도 상태로 관리하고, `다음` 대신 `수정 완료`를 표시한다.
- 수정 모드의 `수정 완료`는 현재 단계만 동일한 규칙으로 검증한 뒤 중간 단계를 반복하지 않고 마지막 확인 단계로 복귀시킨다.
- 확인 단계에서 수정 화면으로 진입할 때 해당 시점의 모든 입력값을 스냅샷으로 보관한다.
- 수정 모드에서 `이전`을 누르면 수정 내용이 저장되지 않는다는 확인 대화상자를 표시하고, 폐기를 확인한 경우 스냅샷을 복원한 뒤 마지막 확인 단계로 돌아간다.
- 수정 모드의 `수정 완료`를 누르면 현재 값을 유지하고 스냅샷을 제거한 뒤 마지막 확인 단계에 변경된 요약 정보를 표시한다.
- 수정 화면에는 현재 수정 단계 안내를 표시하며, 대화상자가 열려 있는 동안 본문 스크롤을 잠근다.
- `다음`을 누르면 현재 단계의 필수값과 형식을 먼저 검증하고, 유효하지 않으면 다음 단계로 이동하지 않는다.
- 위치 단계는 우편번호·도로명 주소·상세 주소, 시설 단계는 편의시설 최소 1개와 체크인·체크아웃 시간, 사진 단계는 사진 최소 1장을 필수 데이터로 검증한다.
- 사진 선택 즉시 로컬 미리보기 썸네일을 표시하고, 1장·2장·3장 이상의 개수에 따라 대표 이미지와 나머지 이미지의 그리드 크기를 반응형으로 조정한다.
- 사진을 처음 등록하면 첫 번째 사진을 기본 대표 사진으로 지정하고, 선택한 사진은 개별 삭제와 추가 등록이 가능해야 하며 삭제 결과도 검증과 확인 단계 요약에 반영한다.
- 대표 사진 상태는 사진 배열 순서와 별도로 관리하며, `대표로 설정`을 선택해도 썸네일 위치와 사진 순서를 변경하지 않는다.
- 새 대표 사진의 버튼은 `대표사진` 상태로 바꾸고 기존 대표 사진의 버튼은 즉시 `대표로 설정`으로 되돌린다.
- 이미지 삭제처럼 원형 아이콘 안에 정밀한 중앙 정렬이 필요한 기호는 글꼴 문자 위치에 의존하지 않고 CSS 도형이나 검증된 아이콘 컴포넌트로 가운데 정렬한다.
- 로컬 이미지 미리보기에 생성한 Object URL은 파일 변경이나 컴포넌트 해제 시 반드시 반환한다.
- 오류가 여러 개여도 화면에는 입력 순서상 첫 번째 유효하지 않은 요소 하나만 오류 상태와 안내 문구를 표시한다.
- 검증 실패 시 첫 번째 유효하지 않은 요소로 포커스를 이동하고 `aria-invalid`, `aria-describedby`, `role="alert"`를 적용한다.
- 오류가 표시된 값을 사용자가 수정하면 해당 오류 표시를 즉시 해제하고 다음 검증 때 다시 확인한다.
- 오류 입력은 빨간 테두리와 빨간 안내 문구를 입력 요소 바로 아래에 표시한다.
- 오류 문구가 추가되어도 같은 행의 다른 입력 요소가 늘어나거나 줄어들지 않도록 폼 그리드는 `align-items: start`, 각 필드는 `align-self: start`를 사용한다.
- 한 줄 입력 요소와 선택 요소는 오류 유무와 관계없이 동일한 고정 높이를 유지하고, 오류 문구는 입력 요소 바깥의 별도 영역에 표시한다.
- 한 행에 배치되는 입력 요소와 동작 버튼은 동일한 높이를 사용한다.
- 전화번호는 숫자만 받아 `010-1234-5678` 형식으로 자동 변환하고 하이픈 포함 13자로 제한한다.

## 비밀번호 정책과 입력 안내

- 비밀번호는 영문, 숫자, 특수문자를 모두 포함한 10자 이상 64자 이하만 허용하고 그 밖의 문자(공백·한글 등)는 받지 않는다.
- 규칙과 안내 문구는 프론트엔드 `frontend/src/utils/passwordPolicy.ts`와 백엔드 `com.tripflow.global.validation.PasswordPolicy` 두 곳에만 두고 판정 결과가 항상 같도록 맞춘다(문구는 각 계층의 기존 표기 관례를 따른다). 한쪽만 바꾸지 않는다.
- 서버 검증은 요청 DTO의 비밀번호 필드에 `@NotBlank`와 `@ValidPassword`를 함께 붙여 처리하고 화면 검증만으로 끝내지 않는다.
- 비밀번호 입력 화면은 직접 input을 만들지 않고 공통 `PasswordFields`를 사용하며, 화면 규격은 `fieldClassName`(인증 모달 `login-modal-field`, 계정 페이지 `host-form-field`)으로 전달한다.
- 비밀번호 입력은 값이 바뀔 때마다 즉시 판정해 정책에 어긋나면 빨간 안내 문구를, 통과하면 `사용 가능한 비밀번호입니다.`를 입력 요소 바로 아래에 표시한다.
- 비밀번호 확인 입력도 값이 바뀔 때마다 판정해 불일치는 빨간 `비밀번호가 일치하지 않습니다.`, 일치는 `비밀번호가 일치합니다.`를 표시한다.
- 서버가 내려준 비밀번호 오류는 실시간 안내보다 먼저 보여주고, 사용자가 값을 수정하면 즉시 실시간 안내로 되돌린다.
- 이전 비밀번호와 같은 값으로는 변경할 수 없다. 비밀번호 초기화는 이전 비밀번호를 알 수 없으므로 실시간 비교를 하지 않고, 제출 시 서버가 `passwordEncoder.matches` 로 검사해 `SAME_AS_CURRENT_PASSWORD` 를 반환한다. 화면은 `이전 비밀번호와 동일하게 변경할 수 없습니다.` 를 비밀번호 입력 아래에 표시하고 인증 단계로 되돌리지 않는다.
- 동일 비밀번호로 거절된 경우 초기화 토큰을 소비하지 않는다. 사용자가 다른 비밀번호로 곧바로 다시 제출할 수 있어야 한다.
- 마이페이지 비밀번호 변경은 현재 비밀번호를 입력받으므로 두 입력값을 화면에서 onChange 실시간 비교해 같은 값이면 즉시 안내하고, 서버에서도 같은 검사를 반복한다.
- 마이페이지 비밀번호 변경을 구현할 때도 이 절을 따르며, 공통 `PasswordFields`에 새 비밀번호와 확인 필드를 맡기고 현재 비밀번호 입력만 화면에서 추가한다. 서버는 새 비밀번호에 `@ValidPassword`를 적용하고 변경 성공 시 기존 리프레시 토큰을 폐기한다.

## 카카오 주소 및 지도

- 주소 검색과 지도 기능은 기존 `HostRegisterPage.tsx`의 카카오 우편번호·지도 구현을 기준으로 동작과 UI를 일관되게 확장한다.
- 주소는 카카오 우편번호 검색 결과의 `roadAddress → autoRoadAddress → address` 우선순위로 선택하고 우편번호와 도로명 주소를 읽기 전용 필드에 자동 입력한다.
- 검색된 주소는 카카오 지도 `services.Geocoder`로 좌표 변환하고 위도·경도를 별도 상태와 향후 저장 데이터에 포함한다.
- 좌표 변환 성공 시 지도를 표시하고 중심과 마커를 해당 좌표로 이동하며, 상세 주소 입력란으로 포커스를 넘긴다.
- 지도에는 지도 유형과 확대·축소 컨트롤을 제공하고 숙소 위치 선택 지도는 임의 드래그로 좌표가 달라지지 않도록 기본적으로 고정한다.
- 주소 재검색 시 기존 지도와 마커를 재사용하되, 단계 이동이나 재마운트로 지도 컨테이너가 바뀌면 새 컨테이너에 지도를 다시 생성한다.
- 지도 컨테이너는 `ResizeObserver`로 크기 변경을 감지해 `relayout` 후 기존 중심을 유지하며 데스크톱과 모바일 높이를 각각 지정한다.
- 우편번호 스크립트 로딩, 지도 SDK 부재, 좌표 변환 실패를 각각 처리하고 사용자에게 재시도 가능한 오류 안내를 표시한다.
- 주소 검색 중에는 중복 실행을 막고 버튼에 로딩 상태를 표시하며, 팝업이 닫히거나 오류가 발생하면 반드시 로딩 상태를 해제한다.
