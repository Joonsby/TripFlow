# TripFlow 작업 및 커밋 규칙

## 커밋 메시지

- 커밋 메시지는 `타입 : 한글 설명` 형식으로 작성한다.
- 타입은 `feat`, `fix`, `refactor`, `docs`, `test`, `chore` 등 Conventional Commits의 의미에 맞게 선택한다.
- 설명은 해당 커밋이 완성하는 기능이나 변경을 간결한 한글로 적는다.

## 커밋 단위

- 프론트엔드(`frontend/`)와 백엔드(`backend/`)의 변경은 한 커밋에 섞지 않는다.
- 각 커밋은 해당 영역만 체크아웃해도 빌드되고 실행 가능한 상태여야 한다.
- 공통 문서나 저장소 설정은 프론트엔드·백엔드 기능 커밋과 분리한다.
- 하나의 기능이 양쪽을 모두 변경하더라도 백엔드 API를 먼저 독립 커밋하고, 그 다음에 해당 API를 사용하는 프론트엔드를 별도 커밋한다.
- 관계없는 기능, 버그 수정, 설정 변경은 각각 되돌릴 수 있도록 별도 커밋한다.

## 커밋 전 검증

- 프론트엔드 커밋 전에 `frontend/`에서 `npm run lint`와 `npm run build`를 실행한다.
- 백엔드 커밋 전에 `backend/`에서 `./gradlew test`를 실행한다.
- 일부 파일만 스테이징한 뒤 `git diff --cached --check`와 `git diff --cached --name-only`로 커밋 범위와 공백 오류를 확인한다.
- 환경 제약으로 검증하지 못한 경우에는 그 사유를 커밋 결과와 함께 명시한다.

## 프론트엔드 페이지 레이아웃

- 새로 만드는 모든 페이지의 콘텐츠 영역은 헤더의 `.header-nav`와 같은 수평 규격을 사용한다.
- 데스크톱과 태블릿에서는 콘텐츠를 가운데 정렬하고 `width: min(1700px, calc(100% - 56px))`를 기준으로 좌우 여백을 각각 28px 확보한다.
- 화면 너비가 820px 이하일 때는 `width: calc(100% - 28px)`를 기준으로 좌우 여백을 각각 14px 확보한다.
- 페이지별로 임의의 `max-width`나 `100vw` 기반 폭을 추가해 헤더와 좌우 경계가 달라지게 하지 않는다.
- 기존 공통 페이지 컨테이너가 있으면 이를 재사용하고, 필요한 경우 헤더 규격을 따르는 공통 레이아웃 클래스로 확장한다.
- 카드, 폼, 지도 등의 내부 콘텐츠는 `width: 100%`, `max-width: 100%`, `min-width: 0`, `box-sizing: border-box`를 적용해 컨테이너 밖으로 넘치거나 가로 스크롤을 만들지 않도록 한다.
