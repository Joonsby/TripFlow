<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tripflow.user.mapper.UserMapper">
    <resultMap id="userResultMap" type="com.tripflow.user.domain.User">
        <id property="userId" column="user_id"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="name" column="name"/>
        <result property="nickname" column="nickname"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="email" column="email"/>
    </resultMap>

    <sql id="userColumns">
        user_id,
        password_hash,
        name,
        nickname,
        phone_number,
        email
    </sql>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM user_info
            WHERE email = #{email}
        )
    </select>

    <select id="existsByPhoneNumber" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM user_info
            WHERE phone_number = #{phoneNumber}
        )
    </select>

    <select id="findByEmail" resultType="com.tripflow.user.domain.User">
        SELECT
            <include refid="userColumns"/>
        FROM user_info
        WHERE email = #{email}
    </select>

    <select id="findByNameAndPhoneNumber" resultMap="userResultMap">
        SELECT
            <include refid="userColumns"/>
        FROM user_info
        WHERE name = #{name}
          AND phone_number = #{phoneNumber}
    </select>

    <insert id="insertUser" parameterType="com.tripflow.user.domain.User" useGeneratedKeys="true" keyProperty="userId" keyColumn="user_id">
        INSERT INTO user_info (
            password_hash,
            name,
            nickname,
            phone_number,
            email,
            join_date
        )
        VALUES (
            #{passwordHash},
            #{name},
            #{nickname},
            #{phoneNumber},
            #{email},
            NOW()
        )
    </insert>

    <update id="updatePassword">
        UPDATE user_info
        SET password_hash = #{passwordHash}
        WHERE user_id = #{userId}
    </update>

    <select id="findById" resultMap="userResultMap">
        SELECT
            <include refid="userColumns"/>
        FROM user_info
        WHERE user_id = #{userId}
    </select>
</mapper>
